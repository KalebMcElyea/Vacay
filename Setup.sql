USE vacay;

-- CREATE TABLE flight
--  (
--   id INT AUTO_INCREMENT,
--   destination VARCHAR(255) NOT NULL UNIQUE,
--   description VARCHAR(255),
--   price DECIMAL(6 , 2) NOT NULL,
--   category VARCHAR(255),
--   PRIMARY KEY (id)
-- ); 

-- CREATE TABLE ferry
--  (
--   id INT AUTO_INCREMENT,
--   destination VARCHAR(255) NOT NULL UNIQUE,
--   description VARCHAR(255),
--   price DECIMAL(6 , 2) NOT NULL,
--   category VARCHAR(255),
--   PRIMARY KEY (id)
-- ); 

-- INSERT INTO flight
-- (destination, price, description, category)
-- VALUES
-- ("Salt Lake City, Utah", 89.99, "The Jazz Suck and the water is salty", "Flight");

-- INSERT INTO ferry
-- (destination, price, description, category)
-- VALUES
-- ("Alaska", 2000.99, "It'sa cold out der", "Ferry");

-- DROP TABLE flight;
-- DROP TABLE ferry; 